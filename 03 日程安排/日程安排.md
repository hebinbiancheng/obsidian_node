# 11/13 - 11/17
- 模型更新1.9，更新输入输出
- 上线dev，test环境，优化公式表格匹配
- 修改线上bug，熟悉starcode项目
- 修改表格公共模块中的rouge算法，优化header_value过长的处理逻辑

# 11/20 - 11/24
- 修改公式解释中参数不合理的问题，参数需在范围内
- 给函数及对应说明表中添加必要的参数个数和必要的参数类型
- 给公式解释中添加参数个数不正确的判断，重新修改错误返回，以及output处理逻辑
- 熟悉pyparsing和bnf
- 使用文档中的bnf范式转换为pyparsing代码（正在进行中）
# 11/27 - 12/01
- 修改公式中有函数和没函数的拼接公式，在公式解析时出错的问题
- 修改公式中带有负号的负数，在公式解析后符号消失的问题
- 之前修改函数和没函数拼接公式时候，没处理完全，留下来的bug
- 修改sql生成模块中在走缓存的时候增加了判断处理，和结果返回中增加了一个字段
- 还看了一下午的缓存的问题
- 修改函数及对应说明表中必要参数个数
- 增加对缓存中sql的正确性判断
- 在函数及对应说明表中增添参数解释的json格式数据
- 修改公式解释中对参数个数的判断逻辑
# 12/04 - 12/08
- 修改公式解析的一些问题
	- ![[Pasted image 20231207160643.png]]
- 对函数及对应说明表的读取方式转为json的方式
- 对公式解释的json结果不合理的问题进行了一些处理
- 对sql生成模块中的错误sql的转化那里进行了处理
- 重写了公式解析
- 松哥说，公式表格匹配的那个项目，想把现在的rouge-l算法转为之前我改的用go写的，但是中间涉及到指针的使用，怕不安全就放弃了
- 之后松哥让我考虑一下rouge-l匹配的，可以根据函数内的表格区域进行相关度匹配
# 12/11 - 12/15
- 添加表格匹配判断的模块
- 优化新的公式解析器的逻辑，推到的开发和测试环境，然后对出现的问题进行修改
- 重新弄一份英文版的函数及对应说明表格，并将其放入公式解释中，重新写了下英文的prompt
- 根据新的公式解析器，重新写了个脚本，将输出转为json，便于模型使用，然后跑了一下公式纠错的数据，把不正确的公式数据记录在文件中
- 给函数及对应说明表加上最大参数个数
- 对公式参数的合理性判断逻辑进行修改，使用正则表达式
- 对更新的range算法进行长表格的测试，平均快四到五秒，但是会发生错误，评分相同的数据可能和原range算法排序不相同，导致生成的prompt不同，模型可能给出的结果不同
# 12/18 - 12/22
- 根据松哥的要求对公式解析器结果进行修改，并去掉有负号时出现的重复的参数
- 对公式中的参数的正确性判断进行修改，主要是true和false进行处理
- 修改rouge算法的评分相同，排序不相同的问题
- 对公式解释中的output进行处理
- 为公式解释3.0做准备
# 12/25 - 12/29
- 完善公式解释3.0
	- 将表格内容存入字典
	- 对公式解析器进行处理，得到函数解释，参数是函数的列表，参数列表
- 对公共模块table进行优化
	- 利用公式中的单元格替换相关性算法中的数据
	- 利用公式中的单元格区域替换相关性算法中的数据
	- 将rouge算法抽取成方法，进行替换
	- 对方法中的sort方法进行优化替换
# 1/2 - 1/5
- 修改合并分支后被覆盖的代码
- 修改table中的range算法，和问题中的单元格和单元格区域的匹配的问题
- 修改公式解释公式不合法的结果输出
- 写了个超时复盘，根据优化方案，重新弄了10%生产环境
# 1/8-1/12
- 公式解释3.0已初步完成
	读取新的函数及对应说明表，并对其中新的内容进行解析
	根据新的prompt，重新组装prompt为列表
	组装完整的初始版本output
	对output中的explain_num，explain_param，dump进行处理
- 修改output的错误json输出
# 1/15 - 1/19
- 公式解释3.0完成
	- 测试数据430，13
	- 正在写测试代码测结果
- 函数转大写
	- 之前预想token解析（忽略错误公式）
	- 测试数据很多通不过
	- 需重新完成通用改写
	- 采用左括号前面的英文字母转大写的方式
- 对output中的错误做处理
- 这周的告警群里，发现几条公式，之前的公式解析器解析的有问题
# 1/22 - 1/26
- 公式大写重写已测试不影响正常使用
- 修改上星期合公共模块的更改时出现的问题
- 分析增加了表格匹配之后正式环境合测试环境的异同
	- 修改表格中的数据的存储方式，主要是header的存储方式
	- 使用单元格匹配时，确保单元格中的内容不是header中的
- 按照方案完成公式解释3.0的测试代码
	- 修改3.0的bug
	- 结论：有微小的提高
	- 错误分析：模型的影响，生成公式的问题（感觉解释的挺清楚的，但是生成公式有点不对）
- 修改公式解析器的bug问题
